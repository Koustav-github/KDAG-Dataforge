# Two synthetic character-level "languages" over a shared semantic core, with a
# controllable overlap parameter so interference from merging can be dialed up.
# TODO: define grammar/vocab pair and the overlap knob.


def make_domain_pair(overlap: float):
    raise NotImplementedError
